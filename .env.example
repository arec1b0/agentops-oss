# =============================================================================
# AgentOps Security Configuration
# =============================================================================

# Admin API key - full access (ingest, read, admin)
# Generate: openssl rand -hex 32
AGENTOPS_ADMIN_KEY=sk-admin-your-secret-key-here

# Ingest-only API key for agents (optional, lighter permissions)
AGENTOPS_INGEST_KEY=sk-ingest-your-agent-key-here
AGENTOPS_INGEST_RATE_LIMIT=1000

# Additional named keys (format: key:scopes:rate_limit)
# AGENTOPS_API_KEY_PROD=sk-prod-xxx:ingest,read:5000
# AGENTOPS_API_KEY_DEV=sk-dev-xxx:ingest:100

# Default rate limit for requests without API key (per minute)
AGENTOPS_DEFAULT_RATE_LIMIT=100

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins
# Use "*" to allow all (NOT recommended for production)
AGENTOPS_CORS_ORIGINS=http://localhost:8501,http://localhost:3000,https://your-dashboard.com

# =============================================================================
# API Documentation
# =============================================================================
# Set to "false" in production to disable /docs and /redoc
AGENTOPS_ENABLE_DOCS=true

# =============================================================================
# ClickHouse Configuration
# =============================================================================
CLICKHOUSE_PASSWORD=

# =============================================================================
# Quick Start (Development)
# =============================================================================
# Leave AGENTOPS_ADMIN_KEY empty for development (auth disabled)
# WARNING: Never deploy without API keys!